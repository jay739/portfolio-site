'use client';

export default function ContactButton() {
  return (
    <a
      href="mailto:contact@jay739.dev"
      className="inline-block px-8 py-3 rounded-full bg-gradient-to-r from-blue-500 to-pink-500 text-white font-bold shadow-lg transform transition hover:scale-105 hover:shadow-xl hover:brightness-110"
    >
      Contact Me
    </a>
  );
} 